---
name: build-fix
description: Diagnose and fix build, bundling, and dependency errors.
origin: ECC (command)
---
# Build Fix Command

Diagnoses and fixes build, bundling, and dependency errors.

## Usage
Invoke when build, test, or lint failures occur. Reads error output, identifies root cause, applies fix.
