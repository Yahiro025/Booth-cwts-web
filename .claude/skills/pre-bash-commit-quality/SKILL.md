---
name: pre-bash-commit-quality
description: Pre-commit quality checks — tests, lint, format.
origin: ECC (hook)
---
# Pre-Commit Quality Hook

Runs quality checks before git commits.

## Checks
- `npm test` — all tests passing
- `npm run lint` — lint clean
- `npm run format` — formatted correctly
