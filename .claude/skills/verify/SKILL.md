---
name: verify
description: Run full verification: tests, lint, and build.
origin: ECC (command)
---
# Verify Command

Runs full verification: tests, lint, and build.

## Usage
```bash
cd frontend && npm test && npm run lint && npm run build
```
Run before committing or deploying.
