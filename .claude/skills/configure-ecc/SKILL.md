---
name: configure-ecc
description: ECC configuration guide and component catalog for this project.
origin: ECC
---
# Configure ECC

This project (Booth-cwts-web) has ECC components installed in `.claude/skills/` and `.claude/rules/`.

## Project Stack
- JavaScript, React 19, Vite 8, Tailwind CSS v4, Zustand, Vitest
- Canvas games (parkour with physics engine, pong)
- Node.js/Express placeholder backend

## DAILY Skills (installed)
`frontend-patterns`, `coding-standards`, `documentation-lookup`, `security-review`, `tdd-workflow`, `error-handling`, `frontend-a11y`

## DAILY Agents (installed)
`planner`, `tdd-guide`, `code-reviewer`, `security-reviewer`, `build-error-resolver`, `docs-lookup`, `react-reviewer`, `react-build-resolver`, `performance-optimizer`, `silent-failure-hunter`

## DAILY Commands (installed)
`plan`, `tdd`, `code-review`, `build-fix`, `verify`, `quality-gate`, `test-coverage`

## DAILY Rules (installed under .claude/rules/)
`common`, `javascript`, `react` (patterns + hooks), `node`

## DAILY Hooks (installed)
`quality-gate`, `pre-bash-commit-quality`, `post-edit-format`, `block-no-verify`, `session-start`

## Extra
`security-guide`

## LIBRARY Components
For full LIBRARY catalog, see `.claude/skills/skill-library/SKILL.md`
