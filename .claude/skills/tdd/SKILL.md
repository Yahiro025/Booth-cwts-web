---
name: tdd
description: Run test-driven development cycle: RED → GREEN → REFACTOR.
origin: ECC (command)
---
# TDD Command

Runs the TDD cycle: RED → GREEN → REFACTOR.

## Usage
Invoke before implementing features. Writes failing test first, then implements code, then refactors while keeping tests green.
